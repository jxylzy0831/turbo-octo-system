# 女性阿奇 A/B/C 本创作工程

目录约定：

- `source/originals/`：不可修改的原始角色本、主持人手册和互动手册
- `analysis/`：只读抽取、分幕索引、时间线、人物设定与冲突清单
- `drafts/`：按轮次保存的写作草稿
- `output/`：最终 DOCX
- `scripts/`：可复现的抽取、核对与制版脚本
- `qa/`：DOCX 渲染图和结构质检结果

原始 `.docx` 与 `.txt` 已统一移入 `source/originals/`。该目录只读使用，不在原文件上直接修改，也不把源文件复制回根目录。
