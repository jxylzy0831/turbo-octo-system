# 《流氓叙事》阿奇角色本工程约定

## 项目目标

在不改动任何原始资料的前提下，创作女性阿奇的 A 本、B 本、C 本。故事事实与时间线以《流氓叙事手册》为最高依据，分幕、信息释放节奏、篇幅、叙述视角、语言气质和 Word 版式以现有角色本为参照。

## 资料优先级

发生矛盾时，按以下顺序处理；不得静默自行圆设定：

1. 用户在当前任务中明确确认的性转阿奇、改版黛利拉及其他新设定。
2. `source/originals/流氓叙事手册.docx` 中的主持人真相、时间线、案件答案和流程。
3. 各角色 A/B/C 本中能够相互印证的事实与分幕信息。
4. `source/originals/流氓叙事阿奇互动手册-清理版.txt` 中的阿奇互动、台词和舞台执行信息。
5. 为补足叙事连续性而新增的内容。

如第 1 项尚未确认，必须先提问；如第 2—4 项互相冲突，必须记录到 `analysis/冲突与待确认.md` 并交由用户决定。

## 原始文件保护

- 所有不可修改的原始资料统一存放在 `source/originals/`。该目录是源文件保护区，不是工作目录。
- `source/originals/` 内的 `.docx` 与 `.txt` 均视为只读原始资料，禁止覆盖、改名、移动、删除或原地另存。
- 读取原始资料时必须使用 `source/originals/<文件名>`；禁止为了兼容旧命令把源文件复制回仓库根目录。
- 若需转换、清洗、解包或提取原始资料，输出只能写入 `analysis/` 或 `qa/`，不得写回 `source/originals/`。
- 所有抽取文本、索引、时间线和分析材料写入 `analysis/`。
- 写作过程稿写入 `drafts/`；仅经确认和质检的最终文件写入 `output/`。
- 自动化脚本写入 `scripts/`，临时渲染和视觉检查写入 `qa/`。
- 生成物必须使用新文件名；最终 DOCX 必须从选定的原角色本副本制作。

## 写作闸门

- 在用户明确确认关键设定，且理解置信度未达到 95% 前，不得撰写阿奇正文。
- 开写前必须完成：资料清单与 SHA-256、各本分幕对照、主持人真相时间线、阿奇已知信息表、黛利拉改版影响表、冲突清单、文风样本分析、A/B/C 信息释放方案。
- 不得用猜测补齐决定人物动机、案件真相、关系结局或身份秘密的关键空白。

## 文本与版式要求

- 事实主线服从主持人手册，但角色本只呈现阿奇在该幕应当知道、回忆或误解的内容。
- 保持原作的人称、句长、意象密度、时间跳切、标题习惯和情绪克制程度；避免现代网络腔、解释性总结和模型化套话。
- 性转不只替换代词；须同步检查称谓、身体经验、社会处境、关系张力、互动台词和他人视角。
- 黛利拉改版造成的所有连锁变化必须逐项追踪到时间线和各幕。
- 最终 A/B/C 本必须逐页渲染检查，无缺字、重叠、断页异常、孤行或样式漂移后方可交付。

## 可追溯性

- 每条关键事实应记录来源文件与可定位的段落/标题。
- 每轮大改保留独立草稿，不覆盖上一轮。
- 最终交付前再次核对 `source/originals/` 中全部原始文件的 SHA-256，确认与初始清单一致。
